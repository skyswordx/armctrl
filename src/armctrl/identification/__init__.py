"""机械臂动力学参数辨识辅助模块。

这个包只负责 armctrl 应该负责的部分：
- 生成安全激励轨迹；
- 调用硬件后端执行关节轨迹；
- 记录 `q / dq / ddq / tau` 数据集；
- 输出给 URDFly、Pinocchio、FIGAROH、FloBaRoID 使用的离线交接文件。

它不在本仓库里重新实现刚体动力学回归矩阵。
回归矩阵、基参数提取和参数优化应优先交给已有工具链完成。
"""

