# protocol

## 这个目录做什么

这里定义全工程共享的数据契约。
只要跨模块传数据，就尽量先落到这个目录里的模型和枚举。

## 主要组件

- `enums.py`：状态枚举、错误码、profile 名称、adapter 类型。
- `errors.py`：统一错误对象。
- `models.py`：请求、响应、状态、teleop 命令等数据模型。

## 实现思路

先定义协议，再让其他模块依赖协议。
这样可以避免：

- CLI 自己拼 JSON；
- adapter 各自返回不同字段；
- GUI 和测试对状态结构理解不一致。

阅读源码时，这个目录通常是最好的入口。
