# Runtime Gateway 暂存草稿

本目录保存暂时收束起来的设计草稿。

这些文件不是当前实施依据。当前实施只看上级目录的：

- `README.md`
- `sysid_integration.md`

暂存原因：

- 当前优先级是先把 SysID 真机链路做好。
- 其他模块的 Agent、EEF、LeRobot、测试策略和通用 contract 文档还不应各自展开阶段路线图。
- 这些内容后续可以恢复，但恢复时必须围绕同一条 runtime gateway 主线，而不是生成多个互相竞争的规划文档。

恢复规则：

- SysID 主链路收口前，不扩大这些草稿为实施规范。
- 恢复时优先抽取接口边界和不变量，删除重复阶段叙事。
- 不把草稿里的未来设想当作当前真机验收要求。
